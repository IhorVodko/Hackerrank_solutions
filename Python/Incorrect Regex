#Problem: https://www.hackerrank.com/challenges/incorrect-regex/problem


import re

for _ in range(int(input())):

    try:
        test = re.compile(input())
        ans = True
    except re.error:
        ans = False
        
    print(ans) 
